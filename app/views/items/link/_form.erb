<% item ||= @item %>

<%= simple_form_for([:event, item], html: {class: 'form-horizontal'}, remote: true, format: :html) do |f| %>
    <%= f.hidden_field :type %>
    <%= f.input :description %>
    <%= f.input :name %>
    <%= f.input :url %>
    <%= f.button :submit %>
<% end %>